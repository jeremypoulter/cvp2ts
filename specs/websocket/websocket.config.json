{
    "spec": "websocket",
    "source": "http://dev.w3.org/html5/websockets/",
    "local": "websocket.html",
    "instances": {
        "WebSocket": "!!window['WebSocket'] && new WebSocket('ws://web-platform.test:8000/echo')",
        "CloseEvent": "!!window['CloseEvent'] && new CloseEvent('close')"
    }
}
